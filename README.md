# Vidhi Khandelwal — Portfolio

Personal portfolio site. Static HTML, CSS and JavaScript — no build step.

- `index.html` — landing page (animated marketing-ops dashboard)
- `portfolio.html` — the full portfolio
- `styles.css` — design system
- `script.js` — interactions
- `photos/` — images

Open `index.html` in a browser to view locally.
